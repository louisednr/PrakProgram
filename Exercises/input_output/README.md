# C++ Input/Output Exercise

To compile the code and execute all three exercise tasks (Command-line, Standard Input, and File Streams) simultaneously, run:

```bash
make
```


To remove the binary and all generated .txt files:
```bash
make clean
```
